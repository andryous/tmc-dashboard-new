// src/lib/api.ts

// This constant holds the backend base URL for all API requests.
// It reads the value from the .env or Render environment variables.
export const API_BASE_URL = import.meta.env.VITE_API_URL;